{% extends 'layout.html.twig' %}

{% block title %}
    About EtyDict | Dictionary and Etymology Explorer
{% endblock %}

{% block body %}
    <div class="container py-5">
        <section class="text-center mb-5">
            <img src="{{ url }}assets/img/etydict-title.webp" alt="EtyDict" width="300" class="my-0"/>
        </section>
        <div class="card p-5 pb-3 mb-5 rounded-4 fs-5">
            <section class="row g-4 align-items-center mb-5 px-3">
                <h1 class="display-5 fw-bold mb-2 text-center">About EtyDict</h1>
                <p class="mb-1">EtyDict is a free online dictionary and etymology browser powered by the largest collection of etymological data.</p>
                <p class="mt-1">EtyDict's dataset currently has 158,664 definitions for 103,831 words and phrases.</p>
            </section>
            <section class="row g-4 mb-5">
                <div class="col-12 col-lg-6 px-3">
                    <h1 class="display-6 fw-bold mb-4 text-center">Get Started</h1>
                    <p>To start exploring, browse the <a class="text-decoration-none" href="{{ url }}dictionary">dictionary</a> or the <a class="text-decoration-none" href="{{ url }}etymology-explorer">etymology</a> page.</p>
                    <p><a class="text-decoration-none" href="{{ url }}account/register">Create an account</a> to save favorite words, see your browsing history across the dictionary and etymology explorer.</p>
                    <p>Already have an account? <a class="text-decoration-none" href="{{ url }}account/login">Sign in</a> to keep your favorite words synced everywhere.</p>
                </div>
                <div class="col-12 col-lg-6">
                    <div class="rounded-4 border shadow p-4 h-100">
                        <h2 class="fw-semibold mb-3 text-center">Quick Tips</h2>
                        <ul class="ps-3 mb-0">
                            <li class="mb-2">Curious what others are exploring? <a class="text-decoration-none" href="{{ url }}dictionary#trending-words">See currently trending and popular words.</a></li>
                            <li class="mb-2">Look for a specific word with the <a class="text-decoration-none" href="{{ url }}/search">Advanced Search</a> function.</li>
                            <li class="mb-2">In the <a class="text-decoration-none" href="{{ url }}etymology">Etymology explorer</a>, words are coloured by their language era.</li>
                            <li class="mb-2">Hover over a word in the <a class="text-decoration-none" href="{{ url }}etymology">Etymology explorer</a> to read its definition.</li>
                        </ul>
                    </div>
                </div>
            </section>
        </div>
        <section class="row g-4 fs-5">
            <div class="col-12 col-xl-11 col-xxl-9 mx-auto" id="contact-card">
                <div class="card border-0 rounded-4 shadow-sm p-4 p-md-5">
                    <div class="text-center mb-4">
                        <h1 class="display-4 fw-bold mb-3">Contact Us</h1>
                        <p class="fs-4 mb-0">You can reach us using the form below.</p>
                        <p class="fs-4">We always welcome feedback and suggestions.</p>
                    </div>
                    <div class="alert mb-4 d-none" role="status" id="contact-status"></div>
                    <form id="contact-form" class="fs-5" novalidate>
                        <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                        <input type="hidden" name="g-recaptcha-response" id="contact-recaptcha-response">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label for="contact-name" class="form-label">Name</label>
                                <input type="text" class="form-control form-control-lg  fs-5" name="name" id="contact-name" placeholder="Your name or username" value="{{ user.username|default('') }}" required>
                            </div>
                            <div class="col-md-6">
                                <label for="contact-email" class="form-label">Email</label>
                                <input type="email" class="form-control form-control-lg  fs-5" name="email" id="contact-email" placeholder="you@example.com" value="{{ user.email|default('') }}" required>
                            </div>
                            <div class="col-12">
                                <label for="contact-subject" class="form-label">Subject</label>
                                <input type="text" class="form-control fs-5" name="subject" id="contact-subject" placeholder="Topic of your your inquiry" maxlength="120" required>
                            </div>
                            <div class="col-12">
                                <label for="contact-message" class="form-label">Message</label>
                                <textarea class="form-control fs-5" rows="5" id="contact-message" name="message" placeholder="Enter your message here" maxlength="1000" aria-describedby="contact-message-count" required></textarea>
                                <div class="d-flex justify-content-between align-items-center small mt-1">
                                    <span>Maximum 1000 characters</span>
                                    <span id="contact-message-count">0 / 1000</span>
                                </div>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-primary btn-lg mt-4" id="contact-submit">Send message</button>
                    </form>
                </div>
            </div>
        </section>
    </div>
{% endblock %}

{% block scripts %}
    {{ parent() }}
{% endblock %}
